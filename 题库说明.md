# 题库说明

## 1. 题库文件

所有题目都放在 `data/questions.json`。每次点击首页「开始游戏」，系统随机抽
10 道题，所以**加题只需要往这个文件里追加对象**，不用改代码。

目前共 73 题：70 道选择题 + 3 道现场题（填空题）。
题目来自三份文档：「百团题库普通」43 题 + 「题目.docx」新增 5 题
（合计 48 题，category = `普通`）、「百团题库gal」25 题（category = `gal`）。
两种形态（服务端版、纯前端版）用的是同一份题库内容。

### 抽题权重

gal 题出现得比普通题少：

- 普通题权重 1，gal 题权重 0.2（代码里 `GAL_WEIGHT = 0.2`）
- 抽取 10 题时，平均约 9 道普通题 + 1 道 gal 题
- 想调整比例，改 `lib/questions.ts` 和 `standalone/index.html` 里的 `GAL_WEIGHT` 即可

## 2. 题目格式

普通选择题：

```json
{
  "id": 30,
  "type": "choice",
  "question": "题干文字？",
  "options": ["选项A", "选项B", "选项C", "选项D"],
  "answer": "C",
  "category": "普通"
}
```

图片选项（选项是图片，可以不给 options）：

```json
{
  "id": 31,
  "type": "choice",
  "question": "以下哪张脸的主人是××？",
  "optionImages": [
    "/questions/xxx-a.jpg",
    "/questions/xxx-b.jpg",
    "/questions/xxx-c.jpg",
    "/questions/xxx-d.jpg"
  ],
  "answer": "A",
  "category": "动漫"
}
```

现场题（填空题）：只展示题目，不由程序判分，现场工作人员口头判定。

```json
{
  "id": 32,
  "type": "fill",
  "question": "题干文字？",
  "images": ["/questions/xxx-1.jpg"],
  "answer": "",
  "category": "综合",
  "countInStats": false
}
```

要点：

- `id` 不能重复，加题时接着最大编号往后写。
- `answer` 填字母（A/B/C/D），要和选项顺序对得上。
- 题干配图放在 `images`，选项配图放在 `optionImages`，路径以 `/` 开头，
  对应 `public` 目录下的文件（例如 `/questions/xxx.jpg` → `public/questions/xxx.jpg`）。
- `type` 为 `fill` 或写了 `"countInStats": false` 的题目不参与判分与正确率统计。
- 新增题目会自动进入抽题池，也会自动出现在「查看题目正确率」页面。

## 3. 当前题库状态

- 70 道选择题的标准答案直接取自各文档末尾给出的答案表。
- 文档里没给答案的 3 道题按现场题收录（不计分、不计入统计）：
  - 第 23 题（六字密码，普通）：6 张人物图
  - 第 31 题（四角色排序，普通）：4 张人物图（按最新要求已去掉题干里的角色名提示）
  - 第 68 题（gal 血缘关系）：纯文字
- 里面有 6 道配图题：
  - 第 23 题（六字密码）题干配图 6 张
  - 第 30 题（哪张脸是阿尔托莉雅）4 张图片选项
  - 第 31 题（四角色排序）题干配图 4 张
  - 第 34 题（偶像大师苍系）4 张图片选项
  - 第 39 题（这张图片出自于）题干配图 2 张
  - 第 48 题（社团管理成员昵称与头像）4 张图片选项
- 加题时记得两个地方都改：`data/questions.json`（服务端版）和
  `standalone/index.html` 里的 `<script id="question-bank">` 那段（纯前端版）。

## 4. 正确率数据

- 统计数据存在 `data/records.json`，导出文件是根目录的 `正确率统计.txt`。
- 统计页只记录选择题；现场题不参与统计。
- 想清零历史数据：把 `data/records.json` 的内容改成 `{"records": {}}`。

## 5. 启动与访问

```bash
npm run dev      # 开发模式，默认 http://localhost:3000
npm run build    # 打包
npm run start    # 用打包结果运行
```

同一局域网内其他设备访问 `http://本机IP:3000` 即可（服务默认监听所有网卡）。
十几台设备同时访问没有问题。

> 注意：项目里的 `npm run dev` 已改成 `next dev --turbo`。
> 原因是这台机器的 Node 24 环境下，Next 14 默认的 webpack 开发模式会卡在
> 「Starting...」起不来，换成 Turbopack 后 1 秒内即可启动，功能不受影响。
